specVersion: "v1beta"
serviceId: "nooreislam"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "nooreislam-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
