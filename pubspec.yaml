name: salam_islam 
description: A professional Islamic app.

version: 1.0.0+1

environment:
  sdk: '>=3.7.0 <4.0.0'
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  audioplayers: ^0.20.1
  cloud_firestore: ^4.15.1
  firebase_core: ^2.24.0
  firebase_remote_config: ^4.2.3
  firebase_auth: ^4.12.0
  flutter_local_notifications: ^18.0.1
  package_info: ^2.0.2
  intl: ^0.20.2

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/timings.json
   
